name: Push 'madebythepinshub/mkdocs-material' image to Docker Hub

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master

env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: madebythepinshub/mkdocs-material

jobs:
  # Run tests.
  # See also https://docs.docker.com/docker-hub/builds/automated-testing/
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          if [ -f docker-compose.test.yml ]; then
            docker-compose --file docker-compose.test.yml build
            docker-compose --file docker-compose.test.yml run sut
          else
            docker build . --file mkdocs-material/Dockerfile
          fi

  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    # Ensure test job passes before pushing image.
    needs: test

    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v2

      - name: Build and push Docker images
        uses: docker/build-push-action@v1.1.0
        with:
          # Username used to log in to a Docker registry. If not set then no login will occur
          username: thepinsteam
          # Password or personal access token used to log in to a Docker registry. If not set then no login will occur
          password: ${{secrets.DOCKER_REGISTRY_ACCESS_KEY}}
          # Docker repository to tag the image with
          repository: madebythepinshub/mkdocs-material
          # Path to the build context
          path: mkdocs-material
          # Whether to push the image
          push: true
